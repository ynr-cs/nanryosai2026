---
title: Account & Terms 開発コンテキスト
tags: [main, context, auth]
status: active
last_updated: 2026-03-19
---

# Account & Terms 開発コンテキスト

本ドキュメントは、マイページ機能 (`account.html`) および 利用規約ページ (`terms.html`) の開発ガイドラインです。

## 1. マイページ (`account.html`) 機能概要

**基本方針**: ユーザーのエンゲージメントを高めるハブとして機能する。認証状態に応じて表示を切り替え、未ログインユーザーにはログインを促す。

### 1.1 認証状態によるフロー

1.  **ゲスト (Guest View)**
    - **UI**: 「ログインが必要です」というメッセージとログインボタンのみを表示。
    - **目的**: 心理的障壁を下げつつ、機能利用の前提としてのログインを促す。
    - **アクション**: ログインボタン押下で Google ログインを実行 (`triggerLogin`)。

2.  **ログイン済み (Authenticated View)**
    - **プロファイル**: 匿名アバター、ニックネーム表示および編集（Firestore `users/{uid}` 保存）、在校生認証バッジ（Custom Claims 判定）。メールアドレス・氏名は完全非表示。
    - **アコーディオン・セクション**:
      - **お気に入り**: 初期状態で非表示。タイトル横に登録数を表示。
      - **注文履歴**: 初期状態で非表示。タイトル横に注文総数を表示。
    - **注文履歴の詳細**:
      - 直近 10 件を表示。
      - 内容: 店舗名、日時、商品詳細、合計金額、ステータスバッジ。
      - **ステータス遷移**: 各注文カードをクリックすると、その注文の詳細ステータス画面 (`pos/status.html?orderId=...`) へ遷移する。
    - **設定 (Settings)**:
      - **呼び出し通知**: FCM トークンの保存リクエスト（`fcmTokens` 配列）。iOS/Mac などの非対応環境では無効化表示。
      - **利用規約リンク**: `terms.html` への遷移。
      - **アカウント削除**: `deleteMyAccount` Callable Function を呼び出し、Firestore ドキュメント・注文データ・Authentication レコードを一括完全消去 (Danger Zone)。

### 1.2 実装詳細要件

- **アコーディオン UI**: 履歴セクションは `max-height` トランジションで開閉。
- **ローディング**: 読み込み中はスケルトンスクリーン (`loading-skeleton`) を表示。
- **ホバーエフェクト**: カード等のインタラクティブ要素に `transform` や `box-shadow` を適用。

## 2. 利用規約ページ (`terms.html`)

- **役割**: サービス利用におけるルールの明示。
- **内容**:
  - **重要事項**: キャンセル不可、受取期限 (15 分)。
  - **BAN 警告**: 違反時はコンピュータ科学部作成サービスからの BAN。
  - **運営情報**: 横浜南陵高校コンピュータ科学部による運営。
  - **免責・禁止事項**: システム的な責任範囲の明記。
- **デザイン**: `style.css` (App Shell) をベースにした統一感のあるデザイン。

## 3. 最新の開発履歴 (2026-01-01 更新)

### 3.1 機能追加

- **ゲスト表示の簡素化 (完了)**: 未ログイン時の情報を隠蔽し、ログイン誘導に特化。
- **利用規約ページの独立 (完了)**: `mobile-order.html` 内の規約コンテンツを独立ページ化し、文言を強化。
- **アカウント削除機能 (完了)**: 物理削除 (`deleteDoc`, `deleteUser`) の実装と再認証フローの考慮。

### 3.2 UI/UX 改善

- **通知設定の堅牢化**: iOS/Mac 判定によるスイッチ無効化、Service Worker 登録待ちによるトークン取得エラーの回避。
- **インタラクション**: 各所へのホバーアニメーション追加。

### 3.3 認証フローの改善 (2026-01-10 更新)

- **認証方式**: `signInWithPopup` を採用 (GitHub Pages 等のクロスドメイン制約回避のため)。
- **アプリ内ブラウザ対策**:
  - LINE/Instagram 等のアプリ内ブラウザで開かれた場合、アラートで警告を表示。
  - 認証処理が 2.5 秒以内に応答しない場合、自動的にタイムアウトして「ログイン画面」を表示 (Blank Page 防止)。
- **動作環境の明記**:
  - Chrome 推奨、iPhone (Safari/Chrome) も動作確認はしたが、原則 Chrome 推奨のスタンス。
  - ログイン不可時の対応として「不具合報告フォーム」へのリンクを設置。
    |

### 3.5 統一ログイン画面 (`login.html`) のUIコンパクト化 (2026-09-02 更新)

- **サイズ最適化**: 見出し（`h1`）のフォントサイズ縮小、ボタンパディング（`padding: 11px 18px`）、コンテナ幅（`max-width: 440px`）および外側パディングのスリム化を実施。
- **ボタンスタイル統一**: 在校生確認や各ステップのセカンダリボタンを専用の `.btn-secondary-custom`（角丸12px、適正パディング）に統一し、巨大なBootstrapデフォルトボタンを排除。
- **カード・案内枠の引き締め**: 在校生限定通知カード（`.student-card`）やブラウザ推奨枠（`.browser-info-box`）の余白を圧縮し、スマホ画面でも1画面にすっきり収まるレイアウトを実現。

### 3.6 マイページ (`account.html`) のUIコンパクト化 (2026-09-02 更新)

- **プロフィールヘッダー**: アバター画像サイズ（`64px x 64px`）、アイコンフォント（`32px`）、パディング（`16px 14px 18px`）、角丸（`20px`）をスリム化。
- **設定メニュー & アコーディオン**: メニュー行パディング（`11px 0`）、セクションタイトル（`0.95rem`）、アコーディオンアイコンサイズを適正化。
- **注文履歴カード**: 注文カードのパディング（`10px 12px`）、アイコン（`34px x 34px`）、フォントサイズを引き締め。
- **削除モーダル & トグル**: アカウント削除確認モーダルのパディング（`18px 20px`）およびトグルスイッチのサイズ（`38px x 20px`）を最適化。

### 3.7 ボトムナビゲーションとのアクティブ状態同期 (2026-09-06 更新)

- **アクティブ表示の純粋化**: 従来はログイン状態時にボトムナビのゲストアイコンへインラインで `style="color: var(--primary-color)"` を付与していたが、別タブ（企画・ステージ等）閲覧時にもアカウントがアクティブに見える混乱を招くため廃止。
- **現在ページの強調に統一**: 他のタブと同様、実際に `account.html` を開いているときのみ `.nav-item.active` が付与されてテーマカラー（紫）でハイライトされる標準挙動に統一。

## 関連ドキュメント
- [[terms_CONTEXT]]: 利用規約の詳細
- [[firebase_CONTEXT]]: 認証基盤
- [[pos\status_CONTEXT]]: 注文ステータス画面（将来的に[[monitor_CONTEXT]]と統合検討）
