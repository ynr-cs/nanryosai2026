---
title: data.js コンテキストドキュメント
tags: [main, context, data]
status: active
last_updated: 2026-09-09
---

# data.js コンテキストドキュメント

このファイルは、`main/data/data.js` 及び関連するデータ構造、同期ロジックについて、今後の開発担当者（AI 含む）に引き継ぐための重要事項をまとめたものです。

## ファイルの役割

- **`main/data/data.js`**: プロジェクト（クラス企画、部活動、有志、特別展示、ステージ）の公式マスターデータ（SSOT）です。
- 開発時および初期表示時のデータソースとして機能します。
- `admin_sync.html` を介して Firestore に同期されますが、同期ロジックには厳格なフィルタリングが存在します。
- 2026-09-09 に公式パンフレット（全10ページ）およびGoogleフォーム提出データ（一次情報）に基づいて全面更生・新旧刷新が完了しました。

## データ構造: `projectData`

`projectData` 配列は、全47企画（クラス企画、部活動、有志企画、委員会、PTA、公的展示）のすべての団体データを含みます。

### 主要フィールド (2026年9月確定仕様)

| フィールド名  | 型 | 説明 | 例 |
| :------------ | :--- | :------------------------------------------------------------ | :----------------------------------------------- |
| `id`          | string | **一意の識別子**。詳細ページの URL パラメータ、画像パス等に使用。 | `301`, `keion`, `homemaking`, `unison` |
| `loginId`     | string | 店舗・団体ログイン用 ID。 | `class301`, `keion` |
| `groupName`   | string | 団体名（クラス名や部活名）。 | `3年1組`, `軽音楽部`, `The Unison. (1-6ピアノ有志)` |
| `name`        | string | 企画名（店名・演目名・展示タイトル）。 | `アキコのひとくちカステラ`, `今日から俺は！` |
| `place`       | string | **確定場所表記**。一覧画面・詳細画面に表示される。 | `中庭 外テント`, `生徒棟 4F 選択教室1` |
| `floor`       | number | 開催フロア（数値）。マップ連携や階層フィルタに使用。 | `1`, `2`, `3`, `4` |
| `roomId`      | string | キャンパスマップ（`campus_map_data.json`）連携用の区画ID。 | `tent_301`, `special_room_select1_4f` |
| `category`    | string | 企画カテゴリ。`"shop"`, `"exhibit"`, `"stage"`。 | `shop` |
| `votingCategory` | string | **[NEW] 南陵グランプリ投票部門**。`"cooking"`, `"shop"`, `"exhibit"`, `"stage"`。 | `cooking` |
| `useMobileOrder` | boolean | **モバイルオーダー対象フラグ**。本年度は **3年1組 (`301`) のみ `true`**。他は全て `false`。 | `true` (`301`), `false` (他) |
| `catchphrase` | string | キャッチコピー（15文字以内推奨）。 | `アキコ監修`, `向井、いつでもいけます！！` |
| `description` | string | 企画紹介文（生徒執筆公式PR文）。 | `...` |
| `instagram`   | string | **[NEW]** 公式/代表者InstagramアカウントID。 | `@_aoi_m__`, `chikara_0809` |
| `allergyInfo` | string | **[NEW]** 調理・食品企画のアレルギー成分表示。 | `卵、はちみつ、小麦粉、牛乳` |
| `contentType` | string | 企画の種類。`menu` (販売あり) または `gallery` (展示・発表)。 | `menu`, `gallery` |
| `menu`        | array | 販売メニューの配列。未確定店舗は空配列 `[]` で保護。 | `[{ name: "...", price: "..." }]` |
| `gallery`     | array | 写真ギャラリー配列。 | `[]` |

### 設計方針 (単一詳細ページ & stageData SSOT)
- **タイムスロット単位の定義**: 各エントリは特定の場所・時間帯の出演枠を表します（`id`: 例 `keion_gym_d1`, `drama_204_d1`, `chorus_d2`, `game_club_after`）。
- **`projectData` との連携**: `groupName`（団体名）を `projectData` の `groupName` と完全一致させることで、`detail.html` は複数日程・複数場所に出演する団体（軽音楽部、吹奏楽部、モダンダンス部、コーラス部、2年4組演劇、社会福祉部、1年有志ピアノなど）の全ステージ枠を自動集約し、**団体ごとに1つの詳細ページ** 内の「スケジュール」タブに美しくタイムライン表示します。
- **後夜祭の扱い**: 後夜祭（15:52〜17:00）は `day: 2`、`place: "体育館"`、`tags: ["Day2", "後夜祭", ...]` として定義され、`stage-list.html` の DAY 2 ガントチャートおよびタイムラインにシームレスに表示されます。
- **企画名・演目名**: 軽音楽部の体育館ライブ演目名は「NANRYOFES」に統一されています。また、有志企画（オタ芸、ダンス、バンド等）は演目・学年ごとに独立した団体として `projectData` に整備されています。

## Firestore 同期ロジック (`admin_sync.html`)

`admin_sync.html` の `syncStores` / `syncItems` 関数によって Firestore と同期されます。

### 重要なフィルタリングルール

- **`contentType: 'menu'` の団体のみ** が `stores` および `items` コレクションに同期されます。
- 展示・発表系 (`gallery`) の団体は、オーダー機能不要のため Firestore には同期されません（現状の仕様）。
- もし展示団体の混雑状況などを Firestore で管理する場合は、このフィルタリングロジックの変更が必要です。

## 2026年度公式データ更生＆刷新 (2026/09/09)

公式パンフレット（全10ページ）および各団体Googleフォーム提出データに基づき、以下の大規模是正を実施しました。

1. **不要・残骸データの完全除去**: 企画一覧に存在しない幽霊団体（`kado`: 華道部, `shashin`: 写真部）を完全削除。
2. **脱落企画の完全復元 (10件新規追加)**: ホームメイキング部 (`homemaking`), 文芸同好会 (`bungei`), 漫画研究部 (`manga`), 生徒会執行部 (`seitokai`), 防災委員 (`bousai`), 書道科 (`shodo`), 美術科 (`bijutsuka`), PTA (`pta`), 選挙コーナー (`senkyo`), めぐみ (`megumi`)。
3. **部活名の更生**: 旧 `science`（科学部）を正式な **「生物部 / ミクロの生き物の世界」**（特別棟3F生物室）へ更生。
4. **テスト用仮企画名の正常化**: 「キャバクラ風カラオケ」→「あさくらソーセージ工房」、「クラブ」→「（競馬）走れ！」、「ナベが震えたケバブ」→「みんな赤ちゃんになれるバブバブケバブ」等。
5. **場所の確定と未確定企画の非表示ハンドリング**: 3年3組おばけやしきを「生徒棟 4F 選択教室1」、めぐみを「生徒棟 2F 3-4教室」に確定。防災委員および選挙コーナーはパンフレット図面上の具体的区画が未確定なため、現段階ではマップ上で非表示（`floor: null`, `roomId: null`, `hiddenOnMap: true`）とし、渡り廊下スポット（`spot_corridor_2f`）もピン除外。企画一覧および投票データとしては維持。
6. **南陵グランプリ投票部門 (`votingCategory`) の全団体導入**: 調理 (8), 販売 (8), 展示 (19), ステージ (12) の計47団体を完全網羅。
7. **モバイルオーダー範囲**: 3年1組 (`301` アキコのひとくちカステラ) のみ `useMobileOrder: true`。未確定店舗のメニューは空配列 `[]` で安全に保護。

## その他注意点

- `generate_hash.js` などのユーティリティスクリプトが存在しますが、これらは本番稼働用ではなく開発ツールです。
- `map.html` はまだ実装されていない（または主要パスにない）可能性があります。`schedule` プロパティを活用したマップ実装が待たれます。

## 現在の課題と対策 (2025/01/10 - Debugging Admin Page)

### 課題: `stores_test` へのデータ移行と `loginId` 欠損

- **現象**: テスト環境 (`stores_test`) で `loginId` が空になる問題が発生。
- **原因**: `stores_test` は初期状態で空であり、画面上のデータ（`data.js` 由来）で上書きしていたが、`data.js` に `loginId` が含まれていない場合、空文字で上書きされていた。
- **対策**:
  1. **`data.js` の更新**: `loginId` フィールドをマスタデータに追加済み。
  2. **Admin ツールの改良**: `admin_sync.html` に **「本番から読込」** ボタンを追加。
- **推奨ワークフロー (データ移行)**:
  1. 「本番から読込」で `stores` (本番) のデータを取得（`loginId` 含む）。
  2. 「テストモード」にチェックを入れる。
  3. 「Firestore 同期」を実行し、取得したデータを `stores_test` に安全にコピーする。

### 課題: 店舗用パスワード（Secret）の管理 [RESOLVED] (2025/01/14)

- **以前の課題**:

  - `admin_sync.html` ではパスワード (`store_secrets`) を設定できず、開発者が手動でハッシュ生成スクリプトを回す必要があった。

- **解決策 (実装済み)**:
  `admin_sync.html` にパスワード設定機能を統合しました。

  1.  **Backend (Cloud Functions)**:

      - `createStoreSecret`: 単一店舗のパスワードを設定。
      - `batchUpdateStoreSecrets`: 複数店舗のパスワードを一括設定（最大 500 件）。
      - **セキュリティ**: どちらも Super Admin (`ynrcs1000@gmail.com`) のみ実行可能。

  2.  **Frontend (`admin_sync.html`)**:
      - **個別設定**: 店舗一覧の各行に「鍵アイコン (🔑)」を追加。クリックでパスワードを設定（デフォルト値は ID と同じ）。
      - **一括同期**: ツールバーに「🔑 全パスワード一括同期」ボタンを追加。表示中の全店舗のパスワードを「ID と同じ値」に一括リセット・保存する。

- **運用**:
  - 新規店舗追加時は、リストに追加した後、その行の鍵アイコンを押すだけでパスワード設定が完了する。
  - テスト環境構築時などは「一括同期」ボタンで全店舗のパスワードを即座に初期化できる。

### 課題: `data.js` 内の不要な `image` プロパティ削除 (2025/01/14)

- **現象**: `data.js` に `image: "images/..."` という記述があったが、これは古い仕様または不要なデータであった。
- **仕様確認**: `index.html` 等のフロントエンド実装では、団体 ID (`id`) に基づいて画像を自動解決している（例: `id="301"` -> `images/301.png`）。
- **問題点**: 誤ったパスやプレースホルダー URL (`placehold.co`) が `image` プロパティに残っていると、同期時に Firestore に不正なデータが書き込まれるリスクがあった。
- **対策**:
  1. **`data.js` の修正**: `image` プロパティを削除。`menu` 内の `imageUrl` は商品画像として必要なので残すが、プレースホルダーは削除（空文字化）。
  2. **`admin_sync.html` の修正**: Firestore 同期ロジックから `image` フィールドの参照を削除。
- **今後の運用**:
  - 団体のメイン画像（サムネイル）は、`images/{id}.png` として配置するだけで自動的に適用される。
  - `data.js` に画像パスを記述する必要はない。

## 2026年度公式データ更生＆刷新 (2026/09/11 フォーム第2弾反映)

Googleフォームで提出された最新回答データ（2026/09/10提出分: 1年1組〜2年2組の計8団体）を取り込み、以下の更新を実施しました。

1. **1年1組 (`101`)**:
   - 企画名表記揺れ是正: パンフレットの「銭安藤」から正式名称 **「銭安堂」** へ修正。
   - キャッチコピー: `安い美味い早い銭安堂`
   - 紹介文: `安さを重視した昔ながらの駄菓子屋をイメージにして安いのを皆さんに知ってもらうべく名前を銭安堂にしました`
   - Instagram: `lxaf2_`
2. **1年3組 (`103`)**:
   - キャッチコピー: `溶ける前に恋しよう`
   - 紹介文: `ホストとキャバ嬢がお客様さまのお悩み等なんでも聞いてくれるよ。\nアイスとジュースを片手に溶ける前に恋してみない？`
   - Instagram: `ynr.1_3`
3. **1年4組 (`104`)**:
   - 企画名更新: パンフレット英語表記「TOY MANIA」から生徒提出の **「トイストーリーマニア」** へ更新。
   - キャッチコピー: `おもちゃの世界へlet's go`
   - 紹介文: `某遊園地にあるトイストーリーマニアとルールは同じです！動く台車に乗って的を打ってもらいます！目指せ！全制覇`
   - Instagram: `kai102011222`
4. **1年5組 (`105`)**:
   - キャッチコピー: `あなたをアリスの世界へご招待`
   - 紹介文: `男子がメイド服、女子が執事服を着る男装女装カフェです。不思議な国のマジックをお楽しみください。おやつや飲み物の種類は計15種類と豊富です。店内にはカラオケも併設しております。バラエティーに富む当店に、ぜひご来店下さい。`
   - Instagram: `kk0621omu`
5. **1年7組 (`107`) のカフェ・販売部門への移行**:
   - 企画名更新: `PINK★MONSTER` から生徒提出の `PINK MONSTER` へ更新。
   - 部門移行: 生徒提出ジャンル（カフェ・チェキ販売）に基づき、展示部門から **販売部門 (`category: "shop"`, `votingCategory: "shop"`)** へ移行。
   - キャッチコピー: `ウチらの平成マジチョベリグ`
   - 紹介文: `PINK MONSTERは平成の懐かしさを感じる魅力的なお店です！ピンクを大々的に表現し、チェキなどの平成を感じられるカフェです！`
   - Instagram: `haruma.s0329`
6. **2年2組 (`202`)**:
   - キャッチコピー: `運命は君の手に`
   - 紹介文: `前園の闇カジノです！ポイントは写真スポットですいちばん可愛いですたぶん他にもいろいろなゲームができますたのしんでください！`
   - Instagram: `si2o_o9`
   - カテゴリ: 体験型アトラクション／ゲームのため展示部門（`exhibit`）を維持し、`フォトスポット` タグを追加。
7. **ゲーム同好会 (`game_club`)**:
   - 企画名更新: 「南陵スマブラ王決定戦2026」から提出名称 **「スマブラ王決定戦2026」** へ更新。
   - 紹介文: `トーナメント方式で南陵高校の在学生(教員を含む)最強のスマブラプレイヤーを決める大会です。`
   - Instagram: `salmon201213`
8. **PTA (`pta`)**:
   - 企画名更新: 「Nanryo Mart」から提出名称 **「南陵Mart」** へ更新。
   - キャッチコピー: `あなたとコンビニ南陵マート`
   - 紹介文: `南陵マート、お菓子、飲み物、パンあります。チェキもやってます‼️`
   - タグ: `["販売", "PTA", "パン", "お菓子", "飲み物", "チェキ"]`

### 累計フォーム反映状況 (2026-09-11 時点)
- **一次情報完全反映済み団体数**: **19団体**（第1弾 11団体 ＋ 第2弾 8団体）
- **南陵グランプリ部門別集計**: 調理 8件, 販売 9件, 展示 18件, ステージ 12件（全47企画）

## 機能公開フラグ (`IS_MAP_ENABLED`)
- `window.IS_MAP_ENABLED`: マップ機能（`map.html`）を一般公開するかどうかの本番フラグ。
- `true` に設定することで、`app-shell.js`（フッター・ドロワーメニュー）、`detail.html`、`projects-list.html`、`stage-list.html` の全マップリンク・バッジが一括で本番有効化される。
- 2026年9月現在、キャンパスマップの完全完成に伴い `true`（常時オン）で本番稼働中。

## 関連ドキュメント
- [[data_sync_CONTEXT]]: 同期メカニズムの詳細
- [[architecture_CONTEXT]]: 全体構成
- `設計書/data/form_submissions.md`: フォーム集計データ仕様書
- `設計書/data/grand_prix_and_voting_spec.md`: 南陵グランプリ投票部門マッピング仕様書


